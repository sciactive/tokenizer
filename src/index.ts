import { Tokenizer } from './Tokenizer.js';
import { sciactive, ranks, jslingua } from './stopwords.js';

export { Tokenizer };
export default Tokenizer;

export const stopwords = {
  sciactive,
  ranks,
  jslingua,
};
